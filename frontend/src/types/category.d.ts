export type CategoryType = {
    _id: string
    title: string
    englishTitle: string
    description: string
    createdAt: string
    updatedAt: string
}

export type InitialValueType = {
    title: string,
    englishTitle: string,
    description: string
}